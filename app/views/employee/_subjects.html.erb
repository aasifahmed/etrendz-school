<div class="label-field-pair">
    <div class="row">
    <div class="col-md-2">
  <label for="subject_name">
    <%= t('subject') %>
  </label>
  </div>
  <div class="col-md-2">
  <div class="text-input-bg">
    <%= select :subject_assignment, :subject_id, @subjects.map { |s| [s.name, s.id] },{ prompt: t('select_a_subject')},{ onChange: "onChangeRequest(this,  '#{url_for(action: :select_department, subject_id: 'paramid')}')"}  %>
  </div>
  </div>
  </div>
</div>
